# Legacy placeholder kept intentionally empty.
# Runtime configuration is in application.yml plus profile-specific application-*.yml files.
# Default profile is H2 via spring.profiles.active=${SPRING_PROFILES_ACTIVE:h2},
# so a fresh clone can run tests/start locally without a preconfigured Kingbase/PostgreSQL instance.
